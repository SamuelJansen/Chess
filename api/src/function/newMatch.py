def newMatch(event) :
    print(f'newMatch(event)')
    print(f'event.name = {event.name}')
    print(f'event.object.name = {event.object.name}')
    print(f'event.application.name = {event.application.name}')
